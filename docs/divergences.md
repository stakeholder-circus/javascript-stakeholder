# Divergences

- No approved runtime divergences today.
- The only intended future divergence is the experimental provider path, and it must stay additive and outside deterministic parity fixtures.
- The local web terminal is presentation-only and must not change the normalized event model.
