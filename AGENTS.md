# javascript-stakeholder AGENTS

1. JavaScript stays in the provider, browser, and web-terminal sidecar lane.
2. Do not use this repo as the deterministic parity template for follower repos.
3. Use the package-manager scripts already defined in the repo for build, test, and lint work.
4. Keep live-provider runtime, encrypted local state, and browser-surface experiments here instead of spreading them into follower parity repos.
5. Preserve normalized JSON and explicit provenance for any deterministic reference outputs emitted here.
6. Missing or deferred behavior must stay fail-fast and documented in `GAPS.md`.
