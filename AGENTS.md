# javascript-stakeholder AGENTS

1. JavaScript ESM only.
2. Run with npm and Bun.
3. Use Node stdlib first and `node:test` unless justified.
4. Wire `core/` submodule and shared schema/fixtures.
5. Expose deterministic mode (`--seed`, `--output-format`) and parity contract.
